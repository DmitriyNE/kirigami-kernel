[package]
name = "arrange2d"
description = "The §6 exact D24 arrangement + boolean kernel (the beast): canonical decomposition, event spine, DCEL, the eight-step boolean, quotient emission (M3)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
certify-core.workspace = true
geom.workspace = true
# The event spine needs the exact number surface directly (Surd carrier points,
# Poly/Sturm/resultant); geom owns the content *types*, lattice owns the numbers.
lattice.workspace = true

[dev-dependencies]
# Stratum-weighted / metamorphic property tests (rigid-motion invariance,
# residual-zero carrier oracle). Same idiom as `lattice`.
proptest = "1"
