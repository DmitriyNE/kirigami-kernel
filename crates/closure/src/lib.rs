#![forbid(unsafe_code)]
//! `closure` — joint geometry (shell tier; M4).
//!
//! Fan/collar (WEDGE, EXT-WEDGE, `D_collar`, the quotient wedge), the Q-clip on
//! the oriented bisector `b_J = s_J(n_A − n_B)`, the `G_i` retained-side clips
//! and CLIP-DOM domain species, the CAP-IN-D24 census, and MITER-FIT (`ℓ_i`,
//! resultant pairing `φ_J`, per-cell branch identities, `ε_φ` as the order
//! sign). Predicate on `G_i`, never the raw diagnostic `H_i`.
