//! Pure arrangement checkers.
//!
//! The ℤ₂² cocycle-closure check (every closed walk returns its bits — a
//! kernel-defect detector no local check sees), the CAP-OUT completeness
//! bijections (components ↔ faces, separating edges ↔ boundary edges, V_∂ ↔
//! emitted vertices), CAP-OUT-LINK with V_∂-membership computation, and
//! `Link_emitted ≅ Link_geometric` as an identity-fixing oriented isomorphism.
//! Implemented at M3e. The DCEL searcher lives in the `arrange2d` crate.
