# Flex Substrate Representation — v0.24 delta (informative)

Companion to `flex-substrate-rep-spec-v0.24-full.md`, **the sole normative artifact**; on any conflict the full text wins. Twenty-first adversarial round, seven items — over-generalized proof types: machinery typed on the coincident A/B case and demanded from every edge in the arrangement.

## Changelog — v0.24

1. **EDGE-OCCUPANCY could not represent a one-source edge** — two "interior-side" signs presuppose both operands have a boundary there, and on a residual arc they collapse (B_L, B_R) = (0,0) and (1,1) into one value: one vs three occupied quadrants, different transverse links, same packet. Schema replaced by the **four adjacent-cell occupancies (A_L, A_R, B_L, B_R) + frame bit** (left = the cross-product side of (t_e, n_Π × t_e); packet frame-covariant); ARRANGEMENT-BITS becomes a projection of the cell labels; MITER-REGION-IDENTITY scoped to the boundary-boundary stratum. Pattern: type-shaped-by-first-constructor / compression-discards-the-decision.
2. **EDGE-IDENTICAL was demanded where no A/B pair exists** — a generic cap residual has neither a coincident partner nor a miter pairing; the cap-to-flank row's "carrier ∧ interval identity" had no constructor in the demanded type: the ledge's default case failed before topology entered the room. **Mode-indexed identity, dispatched by occupancy**: two boundaries ⇒ **PAIR-IDENTICAL** (the renamed type, both mints); one boundary ⇒ **OUTPUT-SOURCE-IDENTICAL** (carrier ∧ interval *containment* ∧ ε vs the source sense; mint ARRANGEMENT-PROVENANCE); zero ⇒ provenance + zero-assertions, no pair identity. Rider: a universally-demanded proof type enumerates its demand strata and exhibits a constructor per stratum. Pattern: one-name-two-theorems / uninhabited-universal-demand.
3. **The vertex bijection demanded emission the edge layer forbade** — suppressed-interior candidates (all incident edges zero-incidence) were simultaneously mandatory (the V_cand bijection) and impossible (SEW-LINK over nonexistent faces). **V_cand / V_∂ split**: CAP-OUT-LINK audits all of V_cand and *computes* membership (proper interval ⇒ ∈ V_∂; full/empty ⇒ ∉, with cross-check defect detectors; disconnected ⇒ reject); **V_∂ ↔ emitted shell vertices**; SEW-LINK over V_∂; miter branch V_cand = V_∂ stated. Pattern: candidate-conflated-with-emitted.
4. **SEW-LINK computed the ideal link and never compared the records** — a→c→b→d crosses while the geometric reconstruction finds the honest cycle and every per-edge count passes. Conclusion restated: **Link_emitted(v) ≅ Link_geometric(v)** — the stored half-edge walk against the geometric sort, an identity-fixing oriented cyclic isomorphism; CAP-OUT-LINK gains the planar twin clause; the occupancy same-side check cited as the positive exemplar. Rider: oracle ∧ audit, never oracle-instead-of-audit. Pattern: oracle-replaces-audit ("a more sophisticated form of optimism").
5. **MITER-OUT proved regularity, not embeddedness** — the nodal cubic (t² − 1, t(t² − 1)) passes EDGE-REG and self-crosses at interior parameters on no inventory. **EDGE-EMB** (diagonal-factored self-intersection system, Sturm; nodes → split → quotient → link, generically reject-toward-band; CLIP-W's graph certificate = the free per-cell discharge; sections do not inherit the parent's embedding) ∧ **EDGE-EDGE** (pairwise, declared coincidences subtracted); CAP-OUT's structural-embeddedness clause (D24 by class; pairwise = the event calculus) states the asymmetry as a theorem. Pattern: local-immersion-for-global-embedding (second conviction).
6. **"Reparametrize and re-certify" was an imperative in a conjunct slot** — [band or fail] wearing differential geometry, one citation-indirection past the rider's grep (scope extended to the name-expansion graph). EDGE-REG verdicts {pass | fail | **stall ⇒ pending, gate-failing as stored**}; **REPARAM** joins §7's compiler verbs (canonical record, supersession, re-certification from scratch). And **ε_φ := the order sign of the monotone correspondence** — σ_B = σ_A³ splits the shipped derivative definition from its endpoint-order mint; the derivative form demotes to a regular-locus theorem. Patterns: workflow-verb-in-truth-slot (second); definition-mint divergence.
7. **Raw second derivatives are gauge, and edge regularity does not license face sectors.** Coincident-ray ties broken by the **invariant jet** — the cleared curvature-numerator comparison (det(e′, e″), |e′|³-weighted, frame-normalized; the D24 sidedness bit derived as its special case; normal-jet ladder to a declared order cap, then osculation-reject; 3D from stored II). And **FACE-GERM**, branch-indexed: cap ⇒ Π + CAP-OUT-LINK cited; flank ⇒ SLAB ∧ CLIP-DOM corner ∧ N_cut; fan ⇒ WEDGE ∧ REG-V; apex ⇒ the exact tangent-cone constructor ([n_A, n_B] ⊥ T); **no branch ⇒ reject** — S = (u, v², uv) has regular boundary curves and a rank-1 germ: edge regularity licenses rays, not sectors, and not through professional association. Patterns: gauge-artifact-as-invariant; boundary-for-body at the germ level.

## Pattern-ledger additions
Type-shaped-by-first-constructor · one-name-two-theorems / uninhabited-universal-demand · candidate-conflated-with-emitted · oracle-replaces-audit · local-immersion-for-global-embedding (second) · workflow-verb-in-truth-slot (second) / definition-mint divergence · gauge-artifact-as-invariant.

## Issue ledger
165 issues, twenty-one rounds. Open: D11, D19/D23, D20, D26, ramp-pair discharge, generalized-cylinder profiles, curved-closure transition patch, conic-arrangement class / D24 envelopes, singular plane-preimage + curve-cusp + osculation events, collapse-operator obligations, COMPSOLID contract (§14). Prior: v0.23 (7) · v0.22 (7) · v0.21 (7) · v0.20 (7) · v0.19 (7) · v0.18 (8) · v0.17 (6) · v0.16 (5) · v0.15 (5) · v0.14 (6) · v0.13 (6) · v0.12 (6) · v0.11 (6) · v0.10 (8) · v0.9 (8) · v0.8 (9) · v0.7 (11) · v0.6 (11) · v0.5 (19) · v0.4 (9) · v0.3 · v0.2 · v0.1.
