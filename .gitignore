# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed: this is an application/kernel, not a published lib.

# Nix / direnv
.direnv/
result
result-*

# Lean / lake
/certify-check/.lake/
/certify-check/build/
/certify-check/lake-packages/

# Claude Code: shared config (.claude/settings.json, .mcp.json) is committed;
# personal overrides are not.
/.claude/settings.local.json

# OS
.DS_Store

# Charon/Aeneas extraction intermediates (models are committed as certify-check/Lattice/*.lean)
*.llbc

# Diagnostics output: the `export` examples regenerate these on demand
# (`cargo run --example {gallery,cone} --features diagnostics`); never committed.
/gallery.html
/cone.html
